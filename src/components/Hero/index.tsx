import { Form, HeroTitle, FormDiv } from './styles'

const Hero = () => (
  <Form>
    <FormDiv>
      <HeroTitle>
        As melhores vagas para tecnologia, design e artes visuais.
      </HeroTitle>
    </FormDiv>
  </Form>
)

export default Hero
